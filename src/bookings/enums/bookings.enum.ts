export enum BookingCategory {
  HOTEL = 'hotel',
  ROOM = 'room',
  TOUR = 'tour',
  FLIGHT = 'flight',
}

export enum BookingStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  CANCELLED = 'cancelled',
  COMPLETED = 'completed',
}